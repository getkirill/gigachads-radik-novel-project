;
import { processor } from "../../Chain";

// sorry for this eldritch horror
export const textToMarkdown = processor((i: string) =>
  
);

const markdownToScriptdownAST = processor((input: Root) => {});
